Database name: "infuseglass" SQL file included

~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
Database connection:

	$host = 'localhost';
	$user = 'root';
	$password = '';
	$database = 'infuseglass';

~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
Controller:
Proccesses, authentication and authorisation will be included here

Model:
This sub-directory includes functions and database connection

View:
.php files for each page, images, fonts and .css
file is included within this sub-directory